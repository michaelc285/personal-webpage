import React from "react";

/**
 * Navbar with img bg
 * Profolio sum && read me
 * About sum && read me
 * Resueme sum && read me
 *
 */

const Home = () => {
  return <div>Home page</div>;
};

export default Home;
